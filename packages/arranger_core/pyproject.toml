[project]
name = "arranger-core"
version = "0.1.0"
dependencies = ["model-backends", "pydantic", "pyyaml", "pretty_midi", "music21", "mido"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
