[project]
name = "arranger-training"
version = "0.1.0"
dependencies = ["dataset-tools", "mido", "pydantic"]

[project.optional-dependencies]
ai = ["miditok==3.0.3", "symusic==0.5.5"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
