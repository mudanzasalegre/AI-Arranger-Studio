# PR-01 - Congelar Epic 15/16

## Fecha

2026-06-25

## Resumen

Se congelo el estado previo a la integracion IA. La base actual queda verificada
en:

```text
Epic 15 - Musical QA baseline
Epic 16 - SongPlan / SectionPlan / PhrasePlan / GrooveMap
```

No se implemento `model_backends`, no se creo `configs/ai_models.yaml` y no se
inicio PR-02.

## Archivos tocados

- `tests/test_song_planner.py`
- `docs/progress/MUSIC_BASELINE.md`
- `docs/progress/PR_01.md`

## Funcionalidad anadida

- No hay cambios de runtime.
- Se reforzo la prueba de exportacion de `SongPlan` para comprobar de forma
  explicita que `SectionPlan`, `PhrasePlan` y `GrooveMap` viajan en
  `project.metadata["song_plan"]`, `song_plan.json` y `arrangement_project.json`.

## Evidencia Epic 15

- `scripts/golden_generate.py` genera 8 presets golden.
- `outputs/golden/golden_summary.json` queda en estado `pass`.
- Cada preset contiene `music_metrics.json`.
- Metricas congeladas:
  - presets: 8
  - total note events: 5504
  - total quality flags: 10
  - score medio heuristico: 4.362
  - validacion: todos `pass`

## Evidencia Epic 16

- `SongPlan` serializa/deserializa via `save_json` / `load_json`.
- `SongPlanner` es determinista por seed.
- `SectionPlan` se guarda en `metadata.song_plan.sections`.
- `PhrasePlan` se guarda en `metadata.song_plan.phrases`.
- `GrooveMap` se guarda en `metadata.song_plan.groove_map`.
- Los generadores reciben el plan por `GenerationContext.song_plan`.
- Todos los presets golden exportan `song_plan.json`.

## Tests anadidos/modificados

- `tests/test_song_planner.py::test_generators_receive_global_song_plan_and_project_metadata_exports_it`

## Comandos ejecutados

```text
python -m pytest tests/test_song_planner.py tests/test_music_baseline.py -q
python -m ruff check apps packages scripts tests
python -m pytest -q
npm --prefix apps/web run lint
python scripts/golden_generate.py
```

Comprobaciones adicionales:

```text
Test-Path packages/model_backends
Test-Path configs/ai_models.yaml
```

## Resultado de comandos

- tests focalizados: 6 passed.
- ruff: pass.
- pytest completo: 82 passed, 1 warning de `StarletteDeprecationWarning` ya existente.
- npm lint: pass.
- smoke musical: pass, 8 presets golden.
- `packages/model_backends`: no existe.
- `configs/ai_models.yaml`: no existe.

## Artifacts generados

- `outputs/golden/golden_summary.json`
- `outputs/golden/golden_summary.md`
- `outputs/golden/<preset>/music_metrics.json`
- `outputs/golden/<preset>/song_plan.json`

## Riesgos pendientes

- El baseline sigue siendo una foto estructural, no una prueba auditiva de calidad.
- Los problemas musicales ya documentados siguen pendientes: narrativa debil,
  comping denso, bateria previsible y poca interaccion entre roles.
- La integracion IA aun no existe; debe empezar en PR-02 con contrato de backends.

## Siguiente PR recomendado

- `PR-02 - Epic 16.5 AI Model Backend Contract`
