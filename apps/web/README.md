# Web app

Next.js frontend for AI Arranger Studio.

## Local commands

```bash
make setup-web
make web
```

Without `make`, run the equivalent commands from the repository root:

```bash
npm --prefix apps/web install
npm --prefix apps/web run dev
```

The full project workflow is implemented in Objective 11. Objective 0 keeps a static starter screen so the frontend can boot.
